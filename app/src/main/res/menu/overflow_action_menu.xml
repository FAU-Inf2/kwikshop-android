<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/overflow_menu"
        android:title="options"
        android:orderInCategory="101"
        app:showAsAction="always"
        android:icon="@drawable/ic_more_vert_white_24dp">

        <menu>

            <!-- If you change the order here, make sure to adjust the order for visibility in ShoppingListActivity -->

            <item android:id="@+id/action_add_recipe"
                app:showAsAction="ifRoom"
                android:visible="true"
                android:title="@string/overflow_action_menu_add_recipes"/>

            <item
                android:id="@+id/share_option"
                app:showAsAction="always"
                android:title="@string/share"/>


            <item android:id="@+id/action_edit_items"
                app:showAsAction="ifRoom"
                android:visible="true"
                android:title="@string/overflow_action_menu_edit" />

            <item android:id="@+id/action_shopping_mode"
                  app:showAsAction="ifRoom"
                  android:visible="true"
                  android:title="@string/overflow_action_menu_shopping_mode" />

            <item android:id="@+id/action_shopping_mode_disable"
                app:showAsAction="ifRoom"
                android:visible="false"
                android:title="@string/overflow_action_menu_shopping_mode_quit" />


        </menu>

    </item>

</menu>